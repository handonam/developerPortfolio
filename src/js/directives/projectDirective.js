'use strict';

var angular = require('angular');

module.exports = [
  '$window',
  function($window) {
    return {
      scope: {},
      link: function(scope, $elem, attr) {
        
      }
    };
  }
];
